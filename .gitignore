# Byte-compiled Python
__pycache__/
*.pyc
*.pyo

# Virtual environments
venv/
.venv/
env/
ENV/

# Secrets / local environment overrides
.env
*.env.local

# Personal voice recording used for cloning - sensitive, and specific to
# whoever set this up locally. Track the folder (see .gitkeep), not the
# audio itself.
voice_samples/*
!voice_samples/.gitkeep

# Model weights / checkpoints, in case any ever end up in-repo instead of
# in the global TTS/torch cache.
*.pt
*.pth
*.onnx
*.safetensors
*.ckpt

# OS cruft
Thumbs.db
.DS_Store
