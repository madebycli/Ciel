# Ciel Linux-native runtime dependencies.
#
# PyTorch is intentionally NOT pinned here. Install the correct build first:
# - AMD target: ROCm-enabled PyTorch matching the host GPU/ROCm stack
# - CPU target: CPU-only PyTorch
# Then install this file. Ciel auto-selects ROCm first and CPU second.
#
# Native Wayland UI dependencies are distro packages, not pip packages:
# GTK, PyGObject, WebKitGTK, gtk-layer-shell and the relevant XDG portal backend.

# Model providers / HTTP
requests>=2.31
websockets>=12.0

# Audio I/O
sounddevice>=0.4.6
soundfile>=0.12
numpy>=1.24

# Local voice
f5-tts>=1.0
pedalboard>=0.9

# Local speech-to-text
faster-whisper>=1.0
