# Project

Ciel is a local-first desktop AI companion framework. The active target is Linux only.

The project should support multiple character packs without forking the runtime. A character changes identity, prompt, voice configuration, visual theme and the list of allowed services. Shared AI, memory, desktop, overlay and voice capabilities stay in reusable subsystems.

The current development branch is `linux-port-clean-start`.
